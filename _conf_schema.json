{
  "trigger_keyword": {
    "description": "触发指令的唤醒词列表（存在其中一个即触发）",
    "type": "list",
    "default": ["我的超能力"],
    "hint": "用户发送的消息匹配列表中任意一个关键词即可触发随机超能力"
  },
  "material_folders": {
    "description": "素材文件夹列表（有序）",
    "type": "list",
    "default": ["超能力", "但是", "主义", "万圣节"],
    "hint": "插件将从这些文件夹中各随机选取一张图片进行拼接，列表顺序决定最终图片的拼接顺序"
  },
  "admin_uids": {
    "description": "管理员 QQ 号列表（不受冷却时间限制）",
    "type": "list",
    "default": [],
    "hint": "填入 QQ 号，每行一个。管理员使用唤醒词不会触发冷却限制"
  }
}
